# BMI Calculator Console App

A simple C++ console application that calculates Body Mass Index (BMI) based on user input.

## How it works
The program asks for the user's weight (kg) and height (m), then calculates the BMI using the formula:

## How to Run
1. Download the `bmi-calculator.cpp` file.
2. Compile it using a C++ compiler (like g++).
3. Run the executable.
